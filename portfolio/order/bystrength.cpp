#include <iostream>

#include "../../aux/security.h"

/* orderByStrength: takes an order and adds it to a linked list where it belongs by strength
 */
security* orderByStrength(security toAdd, security* list) {
    
}